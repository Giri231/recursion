#include<iostream>

using namespace std;

int lcm(int a,int b)
{
   static int m=0;
    m+=b;
    if(m%a==0 && m%b==0)
    
        return m;
    return lcm(a,b);
}

int main()
{
    int a,b;
    cin>>a>>b;
    cout<<lcm(a,b);
    
}
