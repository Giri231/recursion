#include<iostream>

using namespace std;

int evenodd(int st,int n)
{
    if(st>n)
    return 0;
    cout<<st<<endl;
    evenodd(st+2,n);
}

int main()
{
    int n;
    cin>>n;
  cout<< evenodd(1,n);
     cout<<evenodd(2,n);
    return 0;
    
    
}

