#include<iostream>


using namespace std;

int Sum_of_numbers(int n)
{
    if(n==0)
    return 0;
    else
  return Sum_of_numbers(n-1)+n;
}

int main()
{
    int n;
    cin>>n;
    cout<<Sum_of_numbers(n);
    
    return 0;
    
}
