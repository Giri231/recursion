#include<iostream>

using namespace std;

int array(int a[],int st,int l)
{
    if(st>=l)
    return 1;
    cout<<a[st];
    array(a,st+1,l);
}

int main()
{
    int n;
    cin>>n;
   int a[n];
   for(int i=0;i<n;i++)
   {
       cin>>a[i];
   }
   cout<<array(a,0,n);
}
